kind: Service
apiVersion: v1
metadata:
  name: frontend-argo-service
  namespace: house-manager
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: frontend-argo
import {
  TMembersSettingsHook,
  TMembersSettingsProps,
} from "./MembersSettings.types";

const useMembersSettings =
  // eslint-disable-next-line no-empty-pattern
  ({}: TMembersSettingsProps): TMembersSettingsHook => {
    return {};
  };

export default useMembersSettings;
